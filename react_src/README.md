# webpack 4 for react demo

## webpack base config

```js
const contentBase = __dirname + '/react_src'
const entryIndex = __dirname + '/react_src/index.js'
const htmlTemplete = __dirname + '/react_src/index.html'
```

## dev

`npm run devReact`

## build

`npm run buildReact`